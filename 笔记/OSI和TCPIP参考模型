应用层——————为使用osi参考模型的应用提供接口——————————————————————————————HTTP telnet 等
表示层——————负责解码，压缩，加密，保证一个主机发送的数据能被接送发识别
会话层——————在不同主机的应用程序间提供建立，管理和终止会话功能
传输层——————保证数据传输的可靠性（建立，维护和断开一次的数据传输过程），
              提供流量控制功能（控制传输速度节奏）————————————————————————tcp udp
网络层——————根据数据包信息进行寻址，学习路由，实现不同网络建立数据连接—————ipv4 ipv6 
链路层——————在物理链路连接的节点间建立数据连接，提供差错检查，校验数据帧的完整性
物理层——————定义规范电气特性，将数据帧转换成光电信号，通过物理介质传输。
